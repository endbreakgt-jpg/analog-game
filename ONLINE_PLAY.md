# オンライン対戦の起動手順

この版は「部屋1つ」「友人同士」「ホストPCをサーバー」に絞った構成です。
サーバーは標準Nodeだけで動くため、追加ライブラリのインストールは不要です。

## 1. ホストPCにNode.jsを入れる

Node.js LTS版をインストールしてください。
インストール後、PowerShellで以下が表示されれば準備完了です。

```powershell
node --version
npm --version
```

## 2. サーバーを起動する

プロジェクトフォルダで以下を実行します。

```powershell
npm start
```

起動すると以下のようなURLが出ます。

```text
Online game server: http://localhost:3000
```

ホスト本人はブラウザで `http://localhost:3000` を開きます。

## 3. 友人に接続してもらう

インターネット越しの場合は、`http://localhost:3000` を外部公開する必要があります。
友人同士のテストなら、ルーターのポート開放よりもトンネルサービスを使う方が簡単です。

候補:

- Cloudflare Tunnel
- ngrok
- Tailscale Funnel
- ルーターのポート開放 TCP 3000

トンネルサービスを使う場合は、`http://localhost:3000` を公開し、発行されたURLを友人に共有します。

## 4. ロビーで開始する

1. 各プレイヤーがURLを開く
2. 名前を入力して `Join`
3. 最初に参加した人がホストになります
4. 必要ならCPU人数を設定
5. ホストが `Start Game`

ゲーム開始後は、各プレイヤーのブラウザに自分の手札だけ表示されます。
他プレイヤーの手札と未開発特産品は `Hidden` として伏せられます。

## 注意

- ホストPCのブラウザやサーバーを閉じるとゲームは終了します。
- 現時点では部屋は1つだけです。
- 途中参加はゲーム開始前のみです。
- 再読み込みしても同じブラウザなら同じプレイヤーとして戻れます。
