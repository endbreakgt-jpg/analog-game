# CPUプレイヤー実装計画

プレイヤー1人で複数人のテストプレイを容易にするため、単純なヒューリスティックで動作するCPU（AI）プレイヤーを実装します。

## 背景と目的
現在、2人以上のプレイを1人でテストする際、手動で複数人の操作を行う必要があり、ゲームバランスの検証やテストプレイが困難になっています。簡単なルールに基づいて自動で行動を選択するCPUを追加することで、テストプレイの効率化を図ります。

## Proposed Changes

### 1. UIの変更 (index.html, ui.js)
- 初期セットアップ画面（`#setup-overlay`）の「プレイ人数」選択ラジオボタンを廃止し、「人間（Human）」と「CPU」の人数をそれぞれ入力・選択できるUIに変更します。
- 合計人数が1〜5人になるようにバリデーションを設けます。

#### [MODIFY] index.html
#### [MODIFY] ui.js

### 2. ゲームロジックの変更 (game.js, main.js)
- `game.js` の `init` メソッドを修正し、各プレイヤーオブジェクトに `isCpu: boolean` フラグを持たせます。
- `main.js` における `game.onStateChange` （状態変更時のコールバック）にて、現在の操作プレイヤーがCPUであるかを判定し、CPUの場合は自動でアクションを実行するように非同期ループ（`setTimeout`）を組み込みます。

#### [MODIFY] src/game.js
#### [MODIFY] src/main.js

### 3. CPUロジックの実装 (cpu.js)
新規ファイルを作成し、以下のフェーズごとのCPU行動を定義します。CPUの行動は、人間のプレイヤーがボタンを押すのと同じアクション関数（`executeDemand` 等）を呼び出すことで実行します。

- **初期配置 / 無料稼働フェーズ**:
  - 先頭から機械的に特産品を規定枚数（3枚 or 1枚）選択します。
- **手札破棄フェーズ**:
  - 手札が8枚になるように、手札の先頭からランダム（または適当な順序）で超過分を選択して破棄します。
- **メインフェーズ (プレイヤーターン)**:
  - 手札の全ての部分集合（最大8枚なので256通り）に対して、現在公開されている「需要カード」と「固定役」の条件（`ActionValidator.checkRequirements`）を満たすか総当たりでチェックします。
  - **優先度1 (無料アクション)**: 達成可能な固定役があれば達成する。
  - **優先度2 (無料アクション)**: 達成可能な需要カードがあれば達成する。
  - **優先度3 (1AP)**: 稼働中の基盤資源で、まだ加工所を持っておらず、かつ手札に対応する資源があれば「加工所」を建設する。
  - **優先度4 (1AP)**: 手札に基盤資源が2枚以上あり、マーケットに交易品があるなど、実行可能なマーケット交換があれば交換する（手札から適当に選んで交換）。
  - **優先度5**: 実行できる（または有意義な）行動がなければターンを終了する。

#### [NEW] src/cpu.js

## Verification Plan
1. ブラウザでゲームを起動し、人間1人・CPU1人でゲームを開始する。
2. CPUの初期配置、無料稼働、手札破棄が自動で行われることを確認する。
3. CPUのターンにおいて、自動でアクション（需要達成、加工所建設、ターン終了など）が一定の間隔（1秒程度）で実行されることをログおよびUIの挙動から確認する。
4. エラーなく第6ラウンド終了（ゲームオーバー）まで進行できるか確認する。

## User Review Required
- CPUの思考ルーチンは「とにかく達成できるものがあれば達成する」という単純なものになります。より高度な戦略（特定の特産品に合わせて需要を狙う等）は今回は実装しませんが、テストプレイの壁打ち相手としてはこれで十分でしょうか？
